#!/usr/bin/env bash

set -euo pipefail
[ "${TRACE:-false}" = 'true' ] && set -x

WORKDIR=/var/lib/calckey/live/packages/backend

if [ "${RUN_MIGRATION_ON_START:-false}" = 'true' ]; then
    /usr/bin/calckey-migrate
fi

cd "$WORKDIR"

exec node "$WORKDIR/built/index.js"
